# Minimal Burnt-Orange / Charcoal Theme

This repository provides a small static site template with the burnt-orange + charcoal theme.

## Files
- `index.html` — homepage (placeholder content; no slide text included)
- `assets/css/styles.css` — theme CSS (change colors in `:root`)

## Quick deploy (from a fresh repo)
1. Clone locally:
   ```bash
   git clone https://github.com/<your-username>/<your-repo>.git
   cd <your-repo>
